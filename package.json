{
  "name": "codex-proxy-cc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Run Claude Code through a local Anthropic-compatible gateway that preserves Claude Code UX and swaps only model sampling to Codex.",
  "author": "VOIDXAI",
  "license": "MIT",
  "keywords": [
    "agentic-coding",
    "anthropic-compatible",
    "claude-code",
    "codex",
    "developer-tools",
    "gateway",
    "proxy"
  ],
  "engines": {
    "node": ">=22.19.0"
  },
  "bin": {
    "codex-proxy-cc": "./bin/codex-proxy-cc.mjs"
  },
  "scripts": {
    "test": "node --test --test-concurrency=1 tests/*.test.mjs"
  },
  "dependencies": {
    "undici": "^8.0.2"
  }
}
